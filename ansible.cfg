[defaults]
inventory = ./hosts.ini
host_key_checking = False
forks = 5
allow_world_readable_tmpfiles = True
pipelining = True

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_flags = -H -S -i
